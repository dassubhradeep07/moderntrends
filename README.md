# Jewellery Catalog App

Simple offline/online jewellery catalog built with Streamlit.

## Run locally
pip install -r requirements.txt
streamlit run app.py

## Deploy
Upload to GitHub → Streamlit Cloud → Deploy
